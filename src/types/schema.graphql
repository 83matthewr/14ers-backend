type Mountain {
    name: String!
}

type deleteMountainPayload {
    id: ID!
}

type Query {
    mountains: [Mountain]
}

type Mutation {
    createMountain(name: String!): Mountain!
    deleteMountain(id: ID): deleteMountainPayload!
}